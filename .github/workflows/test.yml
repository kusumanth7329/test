name: Integration Tests
 
env:
  host: ${{ secrets.SQL.host }}
  databaseUser: ${{ secrets.SQL.databaseUser}}
  databasePassword: ${{ secrets.SQL.databasePassword }}
  databasePort: ${{ secrets.SQL.databasePort }}
  databaseName: ${{ secrets.SQL.databaseName }}
 
on:
  pull_request:
    branches:
      - main
  
  push:
    branches:
      - kusu
 
jobs:
  integration-test:
    runs-on: ubuntu-latest
 
    steps:
      - name : Install MySQL
        run: |
          sudo apt-get update
          sudo systemctl start mysql.service
          sudo mysql --user=${{ secrets.SQL.databaseUser }} --password=${{ secrets.SQL.databasePassword}} -e "CREATE DATABASE IF NOT EXISTS ${{secrets.SQL.databaseName}};"
 
      - name: Checkout Repository
        uses: actions/checkout@v2
 
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "14"
 
      - name: Install Dependencies
        run: npm install
 
      - name: Run Tests
        run: npm test
