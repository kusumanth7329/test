name: Integration Tests
 
env:
  DB_HOST: ${{ secrets.HOST }}
  DB_USER: ${{ secrets.DATABASEUSERNAME}}
  DB_PASS: ${{ secrets.DATABASEPASSWORD}}
  DB_PORT: ${{ secrets.PORT }}
  DB_NAME: ${{ secrets.DATABASE }}
 
on:
  pull_request:
    branches:
      - main
  
  push:
    branches:
      - kusu
 
jobs:
  integration-test:
    runs-on: ubuntu-latest
 
    steps:
      - name : Install MySQL
        run: |
          sudo apt-get update
          sudo systemctl start mysql.service
          sudo mysql --user=${{ secrets.DATABASEUSERNAME }} --password=${{ secrets.DATABASEPASSWORD }} -e "CREATE DATABASE IF NOT EXISTS ${DB_NAME};"
 
      - name: Checkout Repository
        uses: actions/checkout@v2
 
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "20.x"
 
      - name: Install Dependencies
        run: npm install
 
      - name: Run Tests
        run: npm test
